name: example
description: "Flutter sliding toast example project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_sliding_toast:
    path: ..

dev_dependencies:
  leancode_lint: ^19.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
